# These properties are made available as Java System properties within simulations
bdUrl={{ bd_url }}
bdAPIKey={{ bd_api_key }}
bdUsername={{ bd_username }}
bdPassword={{ bd_password }}

dapUrl={{ dap_url }}
dapUsername={{ dap_username }}
dapPassword={{ dap_password }}

dtsUrl={{ dts_url }}
dtsUsername={{ dts_username }}
dtsPassword={{ dts_password }}
dtsCommKey={{ dts_comm_key }}

# Proxy for CDMI archives (authenticated for read only)
cdmiProxyUrl={{ cdmi_proxy_url }}

# FTP-over-HTTP for CIBER collections
ftpOverHttpUrl={{ ftp_over_http_url }}

# Test collection for DTS extraction
dtsTestPath1={{ dts_test_path1 }}
